# Taxi
***

## 1. Importing the needed libraries
```{r eval=FALSE, Packages}
library(dplyr)
library(ggplot2) # Data visualization
library(VIM)  # Plots missing data
library(mice)
library(ggthemes)
library(scales) # Scaling yaxis
library(caret)
library(ggcorrplot)
library(grid)
library(gridExtra)
library(corrplot)
library(stringr) # Split string on REGEX
```

## 2. Importing and Basic Exploting
```{r}
test <- read.csv('/Users/wafic/Downloads/data/Taxi_Kaggle/test.csv')
train <- read.csv('/Users/wafic/Downloads/data/Taxi_Kaggle/train.csv')
```


```{r}
head(train)
```

```{r}
head(test)
```


```{r}
dim(train)
```

```{r}
train_sample <- train[1:1000,]
head(train_sample)
```


